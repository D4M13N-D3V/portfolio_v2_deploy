# Commissions app demo — UI + core API.
{$CADDY_SITE_SCHEME}{$COMMISSIONS_UI_HOST} {
	encode zstd gzip
	reverse_proxy commissions-ui:3000
}

{$CADDY_SITE_SCHEME}{$COMMISSIONS_API_HOST} {
	encode zstd gzip
	reverse_proxy commissions-core-api:8080
}
