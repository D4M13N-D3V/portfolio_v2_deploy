# Global options + per-app site snippets. Caddy obtains/renews TLS automatically.
# Snippets are added/removed per active profile by scripts/render-caddy.sh, so
# only running apps get a site block (and therefore a certificate).
{
	email {$ACME_EMAIL}
}

import /etc/caddy/sites/*.caddy
