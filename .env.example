# Domain Caddy will serve and obtain a TLS certificate for.
DOMAIN=d4m13n.dev

# Public origin used to build the frontend (baked into the client bundle).
# Same host as DOMAIN, with scheme. For local testing use http://localhost.
SITE_ORIGIN=https://d4m13n.dev

# Email for Let's Encrypt ACME registration / expiry notices.
ACME_EMAIL=damienostler1@outlook.com
